<!DOCTYPE html>
<html>
<head>
    <title>Access Room</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <div class="access-container">
        <div class="access-card">
            <h1><i class="fas fa-door-open"></i> Access Files</h1>
            
            <% if (error) { %>
                <div class="error-message">
                    <i class="fas fa-exclamation-circle"></i> <%= error %>
                </div>
            <% } %>

            <form action="/join" method="GET">
                <div class="input-group">
                    <input type="text" name="code" placeholder="Enter Room Code" required>
                    <button type="submit" class="access-button">
                        <i class="fas fa-arrow-right"></i> Access
                    </button>
                </div>
            </form>
            
            <div class="help-text">
                Don't have a code? <a href="/">Create new room</a>
            </div>
        </div>
    </div>
</body>
</html>